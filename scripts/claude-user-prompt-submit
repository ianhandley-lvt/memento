#!/bin/sh

set -u

memento_command=$(command -v memento 2>/dev/null || true)
if [ -z "$memento_command" ] && [ -x "$HOME/.local/bin/memento" ]; then
  memento_command="$HOME/.local/bin/memento"
fi

if [ -z "$memento_command" ] || ! "$memento_command" hook "$@"
then
  printf '{}\n'
fi

exit 0
